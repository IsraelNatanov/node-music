const mongoose = require('mongoose');
const Joi = require("joi");


let FullFeatureScema = new mongoose.Schema({

    


})

exports.FullFeaturesModel = mongoose.model("fullEventFeatures",FullFeatureScema);